<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= guild.name %> Configuration</title>
    <link rel="stylesheet" href="/css/style.css">
    <script src="/js/config.js" defer></script>
</head>
<body>
    <header>
        <nav>
            <h1><a href="/dashboard">← Back to Guilds</a></h1>
            <span>Configuring: **<%= guild.name %>**</span>
            <a href="/auth/logout">Logout</a>
        </nav>
    </header>

    <main>
        <h2>Settings for <%= guild.name %></h2>
        
        <section class="config-section">
            <h3>General Settings</h3>
            <form id="general-config-form" data-guild-id="<%= guild.id %>">
                <label>
                    Leveling System:
                    <select name="levelingEnabled">
                        <option value="true" <%= config.levelingEnabled ? 'selected' : '' %>>Enabled</option>
                        <option value="false" <%= !config.levelingEnabled ? 'selected' : '' %>>Disabled</option>
                    </select>
                </label>
                
                <label>
                    XP Multiplier Rate:
                    <input type="number" step="0.1" min="0.1" name="xpRate" value="<%= config.xpRate %>">
                </label>
                
                <label>
                    Command Prefix:
                    <input type="text" name="prefix" value="<%= config.prefix %>" maxlength="5">
                </label>
                <button type="submit">Save General Settings</button>
            </form>
            <p id="general-status"></p>
        </section>

        <section class="config-section">
            <h3>Role Rewards (<%= roleRewards.length %>)</h3>
            <% if (roleRewards.length > 0) { %>
                <ul>
                    <% roleRewards.forEach(reward => { %>
                        <li>Level <%= reward.level %>: **<%= reward.role.name %>**</li>
                    <% }); %>
                </ul>
            <% } else { %>
                <p>No role rewards configured. Use the command: <code><%= config.prefix %>addreward 10 @Role</code></p>
            <% } %>
        </section>

        <section class="config-section">
            <h3>Blacklisted Channels (<%= blacklistedChannels.length %>)</h3>
            <p><%= blacklistedChannels.map(c => c.name).join(', ') || 'None' %></p>
            
            <h3>Blacklisted Roles (<%= blacklistedRoles.length %>)</h3>
            <p><%= blacklistedRoles.map(r => r.name).join(', ') || 'None' %></p>
            <p class="note">Blacklists must be managed via bot commands (e.g., <code><%= config.prefix %>blacklistchannel #channel</code>)</p>
        </section>

    </main>
</body>
</html>