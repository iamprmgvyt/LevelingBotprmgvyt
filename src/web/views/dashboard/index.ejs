<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Servers | Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background-color: #2c2f33; color: white; }
        .guild-card { 
            background-color: #23272a; 
            border: 1px solid #4f545c; 
            transition: transform 0.2s; 
            margin-bottom: 20px;
        }
        .guild-card:hover { transform: scale(1.02); border-color: #5865f2; }
        .guild-icon { width: 80px; height: 80px; border-radius: 50%; margin-bottom: 15px; }
        .btn-manage { background-color: #5865f2; border: none; }
        .btn-manage:hover { background-color: #4752c4; }
        .navbar { background-color: #23272a !important; border-bottom: 2px solid #5865f2; }
    </style>
</head>
<body>

    <nav class="navbar navbar-dark bg-dark mb-5">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/">🛡️ Leveling Bot</a>
            <div class="d-flex align-items-center">
                <span class="me-3 text-secondary">Logged in as <strong><%= user.username %></strong></span>
                <a href="/auth/logout" class="btn btn-outline-danger btn-sm">Logout</a>
            </div>
        </div>
    </nav>

    <div class="container">
        <h2 class="mb-4">Select a Server to Manage</h2>
        <p class="text-secondary mb-5">Only servers where you have <strong>Administrator</strong> or <strong>Manage Server</strong> permissions are shown.</p>

        <div class="row">
            <% if (guilds.length > 0) { %>
                <% guilds.forEach(guild => { %>
                    <div class="col-md-4 col-sm-6">
                        <div class="card guild-card p-4 text-center">
                            <% if (guild.icon) { %>
                                <img src="https://cdn.discordapp.com/icons/<%= guild.id %>/<%= guild.icon %>.png" class="guild-icon mx-auto" alt="<%= guild.name %>">
                            <% } else { %>
                                <div class="guild-icon mx-auto bg-secondary d-flex align-items-center justify-content-center">
                                    <span><%= guild.name.charAt(0) %></span>
                                </div>
                            <% } %>
                            
                            <h5 class="card-title text-truncate"><%= guild.name %></h5>
                            
                            <% if (client.guilds.cache.has(guild.id)) { %>
                                <a href="/dashboard/server/<%= guild.id %>" class="btn btn-manage mt-3">Configure Settings</a>
                            <% } else { %>
                                <a href="https://discord.com/api/oauth2/authorize?client_id=<%= client.user.id %>&permissions=8&scope=bot" class="btn btn-outline-success mt-3">Invite Bot</a>
                            <% } %>
                        </div>
                    </div>
                <% }); %>
            <% } else { %>
                <div class="col-12 text-center mt-5">
                    <p class="text-muted">No eligible servers found. Make sure you have Manage Server permissions!</p>
                </div>
            <% } %>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
