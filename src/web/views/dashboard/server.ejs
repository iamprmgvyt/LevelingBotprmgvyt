<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Settings | <%= guild.name %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background-color: #2c2f33; color: white; }
        .settings-card { background-color: #23272a; border: 1px solid #4f545c; border-radius: 8px; padding: 30px; }
        .form-label { font-weight: bold; color: #b9bbbe; }
        .form-control, .form-select { background-color: #40444b; border: none; color: white; }
        .form-control:focus { background-color: #40444b; color: white; border: 1px solid #5865f2; box-shadow: none; }
        .btn-save { background-color: #248046; border: none; width: 100%; font-weight: bold; }
        .btn-save:hover { background-color: #1a6334; }
        .back-link { color: #5865f2; text-decoration: none; }
    </style>
</head>
<body>

    <div class="container mt-5">
        <a href="/dashboard" class="back-link">← Back to Servers</a>
        
        <div class="d-flex align-items-center mt-3 mb-4">
            <% if (guild.icon) { %>
                <img src="https://cdn.discordapp.com/icons/<%= guild.id %>/<%= guild.icon %>.png" width="60" class="rounded-circle me-3">
            <% } %>
            <h1 class="h3 mb-0">Settings for <%= guild.name %></h1>
        </div>

        <% if (typeof success !== 'undefined' || Object.keys(config).length > 0) { %>
            <div id="statusAlert"></div>
        <% } %>

        <div class="settings-card">
            <form action="/dashboard/server/<%= guild.id %>/save" method="POST">
                
                <div class="mb-4">
                    <label class="form-label">Command Prefix</label>
                    <input type="text" name="prefix" class="form-control" value="<%= config.prefix %>" maxlength="5">
                    <div class="form-text text-muted">The character used before commands (e.g. !, ?, ,)</div>
                </div>

                <div class="mb-4 form-check form-switch">
                    <input class="form-check-input" type="checkbox" name="levelingEnabled" id="lvlSwitch" <%= config.levelingEnabled ? 'checked' : '' %>>
                    <label class="form-check-label ms-2" for="lvlSwitch">Enable Leveling System</label>
                </div>

                <div class="mb-4">
                    <label class="form-label">XP Multiplier</label>
                    <select name="xpRate" class="form-select">
                        <option value="0.5" <%= config.xpRate == 0.5 ? 'selected' : '' %>>0.5x (Slow)</option>
                        <option value="1.0" <%= config.xpRate == 1.0 ? 'selected' : '' %>>1.0x (Normal)</option>
                        <option value="1.5" <%= config.xpRate == 1.5 ? 'selected' : '' %>>1.5x (Fast)</option>
                        <option value="2.0" <%= config.xpRate == 2.0 ? 'selected' : '' %>>2.0x (Double)</option>
                    </select>
                </div>

                <div class="mb-4">
                    <label class="form-label">Rank Card / Embed Color</label>
                    <input type="color" name="embedColor" class="form-control form-control-color w-100" value="<%= config.embedColor || '#5865f2' %>">
                    <div class="form-text text-muted">This color is used for your level progress bars and embeds.</div>
                </div>

                <hr class="my-4" style="border-color: #4f545c;">

                <button type="submit" class="btn btn-save py-2">Save Changes</button>
            </form>
        </div>
    </div>

    <script>
        // Check URL for success message
        const urlParams = new URLSearchParams(window.location.search);
        if (urlParams.has('success')) {
            const alertPlaceholder = document.getElementById('statusAlert');
            alertPlaceholder.innerHTML = '<div class="alert alert-success">✅ Settings saved successfully!</div>';
            // Clean URL after 3 seconds
            setTimeout(() => { window.history.replaceState({}, document.title, window.location.pathname); alertPlaceholder.innerHTML = ''; }, 3000);
        }
    </script>
</body>
</html>
