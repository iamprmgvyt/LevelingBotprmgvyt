<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard | Servers</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>body { background-color: #0f172a; }</style>
</head>
<body class="text-slate-200">
    <div class="max-w-6xl mx-auto px-6 py-12">
        <header class="flex justify-between items-center mb-12">
            <div>
                <h1 class="text-4xl font-black text-white tracking-tight">Your Servers</h1>
                <p class="text-slate-400">Select a server to manage leveling settings.</p>
            </div>
            <a href="/auth/logout" class="bg-slate-800 hover:bg-slate-700 px-5 py-2 rounded-lg transition font-medium">Logout</a>
        </header>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            <% guilds.forEach(guild => { %>
                <div class="bg-slate-800/40 border border-slate-700/50 p-6 rounded-3xl flex flex-col items-center text-center hover:border-indigo-500/50 transition-all group">
                    <% if (guild.icon) { %>
                        <img src="https://cdn.discordapp.com/icons/<%= guild.id %>/<%= guild.icon %>.png" class="w-20 h-20 rounded-2xl mb-4 shadow-xl group-hover:scale-105 transition">
                    <% } else { %>
                        <div class="w-20 h-20 bg-indigo-600 rounded-2xl mb-4 flex items-center justify-center text-2xl font-bold"><%= guild.name.charAt(0) %></div>
                    <% } %>
                    <h3 class="text-xl font-bold text-white mb-1"><%= guild.name %></h3>
                    <p class="text-slate-500 text-xs mb-6 uppercase tracking-widest">ID: <%= guild.id %></p>
                    <a href="/dashboard/<%= guild.id %>" class="w-full bg-indigo-600 hover:bg-indigo-500 py-3 rounded-xl font-bold transition shadow-lg shadow-indigo-500/20">
                        Configure
                    </a>
                </div>
            <% }); %>
        </div>
    </div>
</body>
</html>
