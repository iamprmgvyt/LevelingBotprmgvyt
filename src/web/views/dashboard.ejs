<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard | Select Server</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>body { background-color: #0f172a; }</style>
</head>
<body class="text-slate-200 p-8">
    <div class="max-w-6xl mx-auto">
        <div class="flex justify-between items-center mb-10">
            <h1 class="text-3xl font-bold text-white">Select a Server</h1>
            <a href="/" class="text-slate-400 hover:text-white transition">← Back Home</a>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            <% guilds.forEach(guild => { %>
                <div class="bg-slate-800/50 border border-slate-700 p-6 rounded-2xl flex items-center justify-between hover:bg-slate-800 transition">
                    <div class="flex items-center space-x-4">
                        <% if (guild.icon) { %>
                            <img src="https://cdn.discordapp.com/icons/<%= guild.id %>/<%= guild.icon %>.png" class="w-16 h-16 rounded-2xl shadow-lg">
                        <% } else { %>
                            <div class="w-16 h-16 bg-slate-700 rounded-2xl flex items-center justify-center font-bold text-xl">
                                <%= guild.name.charAt(0) %>
                            </div>
                        <% } %>
                        <div>
                            <h3 class="font-bold text-lg text-white"><%= guild.name %></h3>
                            <p class="text-slate-400 text-sm">Server ID: <%= guild.id %></p>
                        </div>
                    </div>
                    <a href="/dashboard/<%= guild.id %>" class="bg-blue-600 hover:bg-blue-500 text-white px-4 py-2 rounded-lg font-semibold transition">
                        Edit
                    </a>
                </div>
            <% }); %>
        </div>
    </div>
</body>
</html>
